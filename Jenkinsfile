pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'npm install'
                echo "build is done!!!!!!" 
            }
        }
        stage('test') { 
            steps {
                 echo "test is done!!!!!" 
            }
        }
    }
}
